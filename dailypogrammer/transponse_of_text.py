#Write a program that takes input text from standard input and outputs the text -- transposed.
#Roughly explained, the transpose of a matrix
#A B C
#D E F
#is given by
#A D
#B E
#C F

#Step 1 : Find the rows and columns 
#Step 2: Transpose

def transpose(sometext):
    text = sometext.split('\n')
    print(text)
    no_of_columns = len(text)
    for word in len(range(sentence)):
       


sometext = """line one
line two"""
transpose(sometext)
        
